#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include <string.h>
//📝 练习 1：数字“拆拆乐”（考察：while 循环、 % 和 / 运算）
//题目描述：
//有一个调皮的机器人，它只会数一个整数每一位上的数字。请你写一个程序，让用户输入一个正整数（比如 1234），然后计算出它每一位数字的和（$1 + 2 + 3 + 4 = 10$）。
//要求：
//
//使用 while 循环来不断拆分数字。
//
//
//处理完后输出总和。
//
//小提示： - 还记得 % 10 吗？它可以帮你拿到数字的“屁股”（最后一位，比如 123 % 10 = 3）。
//
//而 / 10 就像一把小剪刀，可以剪掉最后一位（比如 123 / 10 = 12）。
//int main()
//{
//	int num;
//	printf("请输入一个整数：\n");
//	scanf("%d", &num);
//	while (num > 0)
//	{
//		int a = num % 10;
//		int sum = a;
//		int b = sum + a;
//		int c = num / 10;
//		num = c;
//	}
//	printf("和为：%d\n", b);
//}
//
//int main()
//{
//	int num;
//	int sum = 0; // 1. 把存钱罐放在外面，初始值是 0
//
//	printf("请输入一个整数：\n");
//	if (scanf("%d", &num) != 1) return 0;
//
//	// 如果用户输入负数，我们要先把它变成正数哦
//	if (num < 0) num = -num;
//
//	while (num > 0)
//	{
//		int a = num % 10;  // 拿到最后一位
//		sum = sum + a;     // 2. 核心动作：把新拿到的数字加进原来的存钱罐里
//		num = num / 10;    // 砍掉最后一位
//	}
//
//	printf("和为：%d\n", sum); // 3. 这里的 sum 就能看到最终结果啦
//	return 0;
//}
//📝 练习 2：字母“悄悄话”（考察：char 类型、ASCII 码）
//题目描述：
//为了保护日记的隐私，你想写一个小程序给字母加密。让用户输入一个单词（假设全是小写字母），你把每个字母都变成它在字母表里的后一个字母（比如 'a' 变成 'b'，'z' 变成 'a'）。
//要求：
//
//使用字符数组来存储单词（可以用 scanf("%s", arr)）。
//
//
//使用循环遍历数组进行转换。
//
//小提示： - 在 C 语言里，字符其实就是很小的整数。'a' + 1 就会变成 'b' 哦！
//
//别忘了给 'z' 特殊照顾一下，让它“坐滑梯”回到 'a'。
//int main()
//{
//	char arr[30] = { 0 };
//	printf("请输入你要写的单词：\n");
//	scanf("%s", arr);
//	/*printf("这个单词是：%s", arr);*/
//	char arr2[30] = { 0 };
//	int len = strlen(arr);
//	for (int i = 0; i < len; i++)
//	{
//		if (arr[i] == 'z')
//		{
//			arr2[i] = 'a';
//		}
//		else
//		{
//			arr2[i] = arr[i] + 1;
//		}
//	}
//	printf("加密后的单词为：%s\n", arr2);
//	return 0;
//}
//📝 练习 3：二维阵地“大扫除”（考察：二维数组、嵌套循环）
//题目描述：
//你现在有一个 $3 \times 3$ 的数字小方阵，请你写一段代码，计算出这个方阵里所有数字的总和。
//初始数据：
//C
//int matrix[3][3] = {
//    {1, 2, 3},
//    {4, 5, 6},
//    {7, 8, 9}
//};
//要求： 使用嵌套的 for 循环来遍历这个小方阵。
//小提示： 就像数方格一样，外层循环管“行”，内层循环管“列”，matrix[i][j] 就能精准定位每一个小宝贝啦。
//int main()
//{
//	int a[3][3] = { 1,2,3,4,5,6,7,8,9 };
//	int sum = 0;
//	for (int i = 0; i < 3; i++)
//	{
//		for (int j = 0; j < 3; j++)
//		{
//			sum = sum + a[i][j];
//		}
//	}
//	printf("和为：%d\n", sum);
//	return 0;
//}
//📝 练习 4：班级“小管家”（考察：数组传参、函数应用）
//题目描述：
//老师想请你帮忙算算班里同学的平均分，并且看看有几个同学没及格（低于 60 分）。
//请编写一个函数 void analyze_scores(int arr[], int size) 来实现这个功能。
//要求：
//
//在 main 函数里定义一个数组存 5 个分数。
//
//
//调用函数，在函数内部打印出平均分和不及格的人数。
//
//小提示： - 数组传参的时候，别忘了把数组的长度（size）也一起带上哦，不然函数会迷路的。
//
//算平均分的时候，要把总分定义成 double，结果才精准呢！
//📝 终极 Boss 关卡：班级“风云榜”（考察：冒泡排序、逻辑组合）
//题目描述： 作为小管家，你现在已经能算平均分了。但老师又出难题啦：他想让你把这 5 个同学的分数从高到低排个序，打印出一个“风云榜”。
//
//要求：
//
//编写一个函数 void sort_scores(int arr[], int size)。
//
//在这个函数里，使用经典的 * *冒泡排序（Bubble Sort） * *法，把数组里的分数按从大到小重新排列。
//
//在 main 函数里调用这个函数，并打印出排序后的数组。
//
//小提示（冒泡排序逻辑）：
//
//想象一下，你让分数两两比大小。如果左边的比右边的小，就交换位置（就像刚才学的数组反转魔法）。
//
//这样一轮下来，最小的那个数就像“泡泡”一样沉到了最后。
//
//多来几轮，整个队伍就排好啦！
//
//你需要两层 for 循环： 外层控制一共要跑多少轮，内层负责两两比大小和交换。
//void s_s(int arr[], int size)
//{
//	for (int i = 0; i < size - 1; i++)
//	{
//		for (int j = 0; j < size - 1 - i; j++)
//		{
//			if (arr[j] < arr[j + 1])
//			{
//				int temp = arr[j];
//				arr[j] = arr[j + 1];
//				arr[j + 1] = temp;
//			}
//		}
//	}
//	printf("分数排名为：\n");
//	for (int i = 0; i < size; i++)
//	{
//		printf("%d\n", arr[i]);
//	}
//}
//void a_c(int arr[], int size)
//{
//	int sum = 0;
//	double as = 0;
//	int p = 0;
//	for (int i = 0; i < size; i++)
//	{
//		sum = sum + arr[i];
//	}
//	as = (double)sum / size;
//	for (int i = 0; i < size; i++)
//	{
//		if (arr[i] < 60)
//		{
//			p = p + 1;
//		}
//	}
//	printf("平均分和不及格的人数分别为：%.2lf  %d\n",as , p);
//}
//int main()
//{
//	int size = 1;
//	printf("请输入班级人数：\n");
//	scanf("%d", &size);
//	int arr[100] = { 0 };
//	printf("请输入每个人的分数：\n");
//	for (int i = 0; i < size; i++)
//	{
//		scanf("%d", &arr[i]);
//	}
//	a_c(arr, size);
//	s_s(arr, size);
//	return 0;
//}